name: Docker Image CI

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Build the push Docker image
        run: |
          docker login --username ${{secrets.USER}} --password ${{ secrets.PAT  }} ghcr.io
          docker build . --tag ghcr.io/${{secrets.DIRECTORY}}/toxiq:latest
          docker push ghcr.io/${{secrets.DIRECTORY}}/toxiq:latest
